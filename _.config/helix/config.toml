theme = "ao"
#theme = "bogster_light"
#theme = "base16_transparent"

[editor]
# Activated with Ctrl-x
auto-completion = false
line-number = "relative"

[keys.normal]
# By default, do not yank when changing
A-c = "change_selection"
c = "change_selection_noyank"

[keys.select]
# By default, do not yank when changing
A-c = "change_selection"
c = "change_selection_noyank"
